// An expression is ref transparent if it can be replaced with its value without changing the program's behaviour

// Expression is a combo of consts, vars, operations and funcs that is interpreted and COMPUTED to produce another value

// Value => numerical entity
